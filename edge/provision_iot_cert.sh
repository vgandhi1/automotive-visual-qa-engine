#!/usr/bin/env bash
# Placeholder: provision X.509 certs via AWS IoT CreateKeysAndCertificate.
# In production, store private keys in a hardware TPM or AWS IoT fleet provisioning.
set -euo pipefail
echo "TODO: Use AWS CLI aws iot create-keys-and-certificate and attach a thing policy."
echo "See cloud/iot_core/device_policy.json and plan.md section 10."
